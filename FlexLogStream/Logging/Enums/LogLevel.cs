namespace FlexLogStream.Logging.Enums;

/// <summary>
/// LogLevel: An enum to represent the severity level of a log message (e.g., Information, Warning, Error).
/// </summary>  
public enum LogLevel
{
    Information = 0,
    Warning = 1,
    Error = 2,
    Critical = 3

}
